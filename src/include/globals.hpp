#pragma once

#include <map>
#include <string>

/**
* @brief Type representing a timestamp and its components.
*
* In this program, it consists of only
* mm, ss and ms members to represent a song
* timestamp.
*/
using tsmap = std::map<std::string, long>;